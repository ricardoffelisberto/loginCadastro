'use strict';

describe('myApp.usuario module', function() {

  beforeEach(module('myApp.usuario'));

  describe('usuario controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var usuarioCtrl = $controller('UsuarioCtrl');
      expect(usuarioCtrl).toBeDefined();
    }));

  });
});