'use strict';

describe('myApp.cadastro module', function() {

    beforeEach(module('myApp.cadastro'));

    describe('cadastro controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var cadastroCtrl = $controller('CadastroCtrl');
            expect(cadastroCtrl).toBeDefined();
        }));

    });
});